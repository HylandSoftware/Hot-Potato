400InvalidFIQLQuery:
  description: Response when FIQL query is invalid or not supported.
  content:
    application/problem+json:
      schema:
        $ref: '../models/ProblemModel.yaml#/ProblemModel'
      example:
        title: The workItems query is invalid
        status: 400
        instance: /example-resource
