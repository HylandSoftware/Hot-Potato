workItemType_path:
  in: path
  name: workItemType
  description: The type of work item the system task can execute on.
  required: true
  schema:
    $ref: '../enums/WorkItemTypeEnum.yaml#/WorkItemTypeEnum'

workItemType_query:
  in: query
  name: workItemType
  description: The type of work item the system task can execute on.
  required: true
  schema:
    $ref: '../enums/WorkItemTypeEnum.yaml#/WorkItemTypeEnum'
