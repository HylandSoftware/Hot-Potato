DocumentTypeModel:
  description: Information about a document type
  properties:
    type:
      description: Determines the type of identifier.
      $ref: '../enums/IdTypeEnum.yaml#/IdTypeEnum'
      example:
        - SpecifiedId
    id:
      description: ID of the document type if type is SpecifiedId
      type: string
