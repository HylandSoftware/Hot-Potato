PostDataItemModel:
  description: Name / values pair containing in the post data of a HTML form
  type: object
  properties:
    name:
      description: Name of the item in the post data
      type: string

    values:
      description: List of values for the item in the post data
      type: array
      items:
        type: string
  required:
  - name
  - values
