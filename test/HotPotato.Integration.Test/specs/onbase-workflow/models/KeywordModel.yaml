KeywordModel:
  description: Represents a keyword
  type: object
  properties:
    typeId:
      description: The id of the keyword type for the keyword value
      type: string
    values:
      description: List of keyword values
      $ref: 'KeywordValueModel.yaml#/KeywordValueModel'        
