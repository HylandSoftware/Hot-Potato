QueueQueryResultModel:
  description: |-
    Contains the results of a queue query.
  properties:
    displayColumns:
      description: |-
        List of display columns returned from executing a query
      type: array
      items:
        $ref: 'DisplayColumnConfigurationModel.yaml#/DisplayColumnConfigurationModel'
    items:
      description: |-
        List of work items returned from executing a query.
      type: array
      items:
        $ref: 'QueueQueryWorkItemResultModel.yaml#/QueueQueryWorkItemResultModel'
    filterId:
      description:
        ID of the filter that was applied to the items, or zero if no filter was applied
      type: string
