DocumentTypeGroupModel:
  description: Information about a document type group
  properties:
    type:
      description: Determines the type of identifier.
      $ref: '../enums/IdTypeEnum.yaml#/IdTypeEnum'
      example:
        - SpecifiedId
    id:
      description: ID of the document type group if type is SpecifiedId
      type: string
